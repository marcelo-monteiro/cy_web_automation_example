{
  "scripts": {
    "open": "yarn run cypress open",
    "tests:report": "yarn cypress run --env allure=true --browser chrome --config video=true"
  },
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^13.1.0",
    "@cypress/browserify-preprocessor": "^3.0.2",
    "cypress": "^11.0.0",
    "yamljs": "^0.3.0",
    "@shelex/cypress-allure-plugin": "^2.34.0"
  }
}
